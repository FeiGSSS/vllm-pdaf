#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PAP_ROOT="${PAP_ROOT:-$(cd "${SCRIPT_DIR}/../.." && pwd)}"
BENCHMARK_SCRIPT="${BENCHMARK_SCRIPT:-/home/fei/research/PD/test/baseline/run_benchmark.sh}"

unset http_proxy https_proxy HTTP_PROXY HTTPS_PROXY all_proxy ALL_PROXY

export PAP_ROOT
export VLLM_BIN="${VLLM_BIN:-${PAP_ROOT}/.venv/bin/vllm}"
export PYTHON_BIN="${PYTHON_BIN:-${PAP_ROOT}/.venv/bin/python}"
export MODEL_PATH="${MODEL_PATH:-/data/ssd1/llm-models/Qwen3-32B}"
export PREFIX_LEN="${PREFIX_LEN:-0}"
export PAP_TP_SIZE="${PAP_TP_SIZE:-2}"
export MAX_MODEL_LEN="${MAX_MODEL_LEN:-64}"
export MAX_NUM_SEQS="${MAX_NUM_SEQS:-512}"
export MAX_NUM_BATCHED_TOKENS="${MAX_NUM_BATCHED_TOKENS:-16384}"
export PAP_PREFILL_GPU_MEMORY_UTILIZATION="${PAP_PREFILL_GPU_MEMORY_UTILIZATION:-0.76}"
export PAP_PROJECTION_GPU_MEMORY_UTILIZATION="${PAP_PROJECTION_GPU_MEMORY_UTILIZATION:-0.76}"
export PAP_ENABLE_MPS="${PAP_ENABLE_MPS:-0}"
export PAP_OFFLOAD_EXEC_TRANSPORT="${PAP_OFFLOAD_EXEC_TRANSPORT:-nixl_mailbox}"
export PAP_DIRECT_MAILBOX_OUTPUT="${PAP_DIRECT_MAILBOX_OUTPUT:-1}"
export PAP_NIXL_MAILBOX_SLOT_COUNT="${PAP_NIXL_MAILBOX_SLOT_COUNT:-2}"
export PAP_NIXL_MAILBOX_RECV_SLOT_COUNT="${PAP_NIXL_MAILBOX_RECV_SLOT_COUNT:-2}"
export PAP_NIXL_MAILBOX_ASYNC_SEND_SLOTS="${PAP_NIXL_MAILBOX_ASYNC_SEND_SLOTS:-1}"
export PAP_OFFLOAD_EXEC_TRACE="${PAP_OFFLOAD_EXEC_TRACE:-1}"
export PAP_ATTENTION_LOCAL_PAGED_CACHE="${PAP_ATTENTION_LOCAL_PAGED_CACHE:-1}"
export PAP_OFFLOAD_EXEC_USE_PAGED_FLASH_ATTN="${PAP_OFFLOAD_EXEC_USE_PAGED_FLASH_ATTN:-1}"
export PAP_RUNNER_MICROBATCH_COUNT="${PAP_RUNNER_MICROBATCH_COUNT:-3}"
export PAP_RUNNER_MICROBATCH_DECODE_THRESHOLD="${PAP_RUNNER_MICROBATCH_DECODE_THRESHOLD:-12}"
export VLLM_USE_FLASHINFER_SAMPLER="${VLLM_USE_FLASHINFER_SAMPLER:-0}"
export VLLM_QWEN3_LAYER_PROFILE="${VLLM_QWEN3_LAYER_PROFILE:-1}"
export VLLM_QWEN3_LAYER_PROFILE_ASYNC="${VLLM_QWEN3_LAYER_PROFILE_ASYNC:-1}"
export VLLM_QWEN3_LAYER_PROFILE_ASYNC_FLUSH_THRESHOLD="${VLLM_QWEN3_LAYER_PROFILE_ASYNC_FLUSH_THRESHOLD:-2048}"

exec bash "${BENCHMARK_SCRIPT}" \
  --mode pap \
  --topology "${PAP_TESTBED_TOPOLOGY:-1pa1p}" \
  --input-lens "${PAP_TESTBED_INPUT_LEN:-32}" \
  --output-lens "${PAP_TESTBED_OUTPUT_LEN:-16}" \
  --qps "${PAP_TESTBED_QPS:-256}" \
  --num-prompts "${PAP_TESTBED_NUM_PROMPTS:-128}" \
  --model "${MODEL_PATH}" \
  --proxy-port "${PAP_PROXY_PORT:-9400}"
